apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: auth-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: avishkarlakade/auth-frontend:latest
          ports:
            - containerPort: 80
